workflows-backup/

# Pro Pack SOURCE (readable) - keep local, only the built zip in /files/ is published
packs/

# local screenshots / scratch
*.jpeg
.playwright-mcp/
nul

# Internal docs - keep local, do NOT publish
CLAUDE.md
.claude/
docs/sales-playbook.md
docs/service-agreement.md
docs/linkedin-posts.md
docs/linkedin-profile.md
docs/twitter-content-30days.md
docs/tbe-spec-system-architecture.md
__pycache__/
.DS_Store
*.log
.env
.env.local

# paid product zip - served only via n8n gated download, never public
files/*.zip

# New-direction planning documents (internal, never publish)
new direction/
# Internal business docs (public repo - keep local only)
docs-internal/
# scratch screenshots
shot*.png
thankyou-pro-verified.png

# real-video-pipeline build artifacts (never commit)
app/node_modules/
app/dist/
render-backend/.env
render-backend/**/__pycache__/
render-backend/.pytest_cache/
# generated CI media (uploaded as GitHub Actions artifacts, never committed)
ci-artifacts/

# coverage outputs (regenerated by CI/local runs)
render-backend/.coverage
render-backend/coverage.json
render-backend/coverage.xml
render-backend/test-report.xml

# Project One local media + artifacts — NEVER commit footage or analysis
project-one-local/
